export type Partner = string;

export type PipelineStep = {
  step: string;
  icon: string;
  title: string;
  detail: string;
};

export type Spotlight = {
  title: string;
  image: string;
  alt: string;
  detail: string;
};

export type Integration = {
  name: string;
  detail: string;
};
